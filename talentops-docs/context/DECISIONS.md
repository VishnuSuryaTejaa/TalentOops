# DECISIONS.md — dated log of resolved/overridden `[ASSUME: ...]` calls. Do not re-litigate; override requires a new dated entry.

| Date | ID | Decision | Origin | Status |
|---|---|---|---|---|
| 2026-07-06 | D1 | Node/Express = integration gateway (Recall.ai/Gmail/Calendar webhooks + audio relay); FastAPI = LangGraph agent runtime | 02-ARCHITECTURE §2.1 | **OVERRIDDEN by D10 (2026-07-06)** |
| 2026-07-06 | D2 | User retains offer authority; offer emails require explicit user approval (`requires_user_approval: true`) | 01-PRD §1.4 | Default adopted |
| 2026-07-06 | D3 | v1 sources from a provided resume corpus; no external job-board integration | 01-PRD §1.4 | Default adopted |
| 2026-07-06 | D4 | One role per pipeline instance; multiple instances may run in parallel | 01-PRD §1.4 | Default adopted |
| 2026-07-06 | D5 | Analytics/Scorecard synthesis uses OpenRouter/Nemotron (not latency-critical) | 02-ARCHITECTURE §2.2 | **SUPERSEDED by D17** (executor now Ollama/Llama 3.1; async placement unchanged) |
| 2026-07-06 | D6 | Sprints are 2 weeks, 2–3 engineers; voice loop lands mid-project (S4) | 05-SPRINT-PLAN | Default adopted |
| 2026-07-06 | D7 | Interviews are verbal only — no live coding/IDE assessment in v1 | 01-PRD §1.4 | Default adopted |
| 2026-07-06 | D8 | Rubric timeline: structured rubric persisted at goal intake; frozen (immutable) from first Interviewer sub-agent dispatch for the role | Verification pass — resolved timing contradiction across 02/03/05/GLOSSARY | Adopted |
| 2026-07-06 | D9 | Manager Agent is the sole issuer of Communication `task.assign`; Scheduling's candidate email trigger is routed via Manager Agent, never sub-agent→sub-agent | Verification pass — resolved trigger contradiction across 02/03/PROJECT_CONTEXT | Adopted |
| 2026-07-06 | D10 | Mono-stack = Pure Python: FastAPI native async + `asyncio` background tasks; Node/Express, Celery, Redis eliminated. Rationale: single serialization domain, LangGraph's canonical runtime, ML-native tooling | Architectural override (user directive) | Adopted — **overrides D1** |
| 2026-07-06 | D11 | Voice loop = native multimodal realtime model (audio-in/audio-out, server VAD, native barge-in, target <300 ms; reference: GPT-4o Realtime API). Recall.ai retained as meeting transport only. Silero VAD, Whisper live STT, ElevenLabs TTS dropped from live path. Transcripts from realtime dual-channel transcription events; [ASSUME: optional async batch re-transcription for audit fidelity] | Architectural override (user directive) — supersedes original voice-stack list | Adopted |
| 2026-07-06 | D12 | Pre-Flight Sandbox = 2-min non-graded calibration mode of the Interviewer sub-agent's own candidate-context session (no new speaker identity — voice ownership rule preserved); sandbox audio/transcript excluded from evaluation; telemetry gate may trigger reschedule | Feature injection (user directive) | Adopted |
| 2026-07-06 | D13 | Extractive Evaluation mandatory in Analytics/Scorecard: verbatim quotes (char offsets, speaker) substring-validated against immutable transcript BEFORE scoring; zero validated quotes → `insufficient_evidence`, never inferred. AGENT_CONTRACTS.json bumped to v1.1.0 (scorecard `evidence_quotes` + `demonstrated_level`) | Architectural override (user directive) — unlocks contract change | Adopted |
| 2026-07-06 | D14 | Dynamic Context Injection: Manager Agent scrapes employer-controlled allowlisted sources only (domain/blog/GitHub org, robots.txt-compliant) → distilled hidden-context appendix → enriched JD embedding, frozen with rubric; scraped content treated as untrusted data | Feature injection (user directive) | Adopted — distillation executor changed to Ollama/Llama 3.1 by D17 |
| 2026-07-06 | D15 | Fairness & Bias Lens: demographics are optional, self-reported, collected post-interview; segregated schema with RLS denying all agent roles; aggregate-only k-anonymized heatmap (cohort × question difficulty) + drift alerts; never in any agent context or scoring path | Feature injection (user directive) | Adopted |
| 2026-07-06 | D16 | Prosody policy: audio-native model's paralinguistic perception used for turn-taking/flow only — never as evaluation evidence; all scoring transcript-text-grounded (bias containment for accent/speech-pattern effects) | Consequence of D11 — new risk class R13 | Adopted — **narrowed by D17** (STT boundary restored; policy retained as invariant: scoring stays transcript-text-grounded) |
| 2026-07-07 | D17 | **The Zero-Cost Local AI Pivot** — premium APIs eliminated; $0 base-cost, self-hosted/open-source ecosystem. Mappings: Recall.ai → **WebRTC** (self-hosted OSS Google Meet bot: meeting joins + audio bridging); OpenRouter/Nemotron → **locally hosted Llama 3.1 via Ollama** (all heavy reasoning: frozen rubrics, per-candidate interview briefs, hidden-context distillation, Extractive Evaluation); GPT-4o Realtime + ElevenLabs → **Optimized Free-Chain** = Silero VAD + Groq Whisper (STT) + Groq Llama (reasoning) + self-hosted **Kokoro TTS** — unified as THE voice stack for both voice contexts (native-multimodal candidates Moshi/Qwen2-Audio explicitly rejected). Latency target revised: <300 ms dropped (GPT-4o-specific) → **≤1.5 s P50 / ≤2.5 s P95** per turn. Risk register: R1 → free-chain latency (mitigate: local hardware + Kokoro instance tuning); R2 → local server overload (mitigate: hard-cap interview times; Ollama reserved for async/offline tasks). Caveat logged: Groq is a hosted API — "$0 base cost" holds only within Groq free tier [ASSUME: acceptable; local-Whisper fallback if quota binds]. Supersedes D11's voice-stack selection; D11's dual-channel-transcription assumption replaced by chain-native Whisper transcripts | Architectural pivot (user directive) | Adopted — **supersedes D11 voice stack; overrides R1/R2 definitions** |
| 2026-07-13 | D18 | **The API-Based Free Stack Migration** — self-hosted AI services fully eliminated; zero local GPU requirement. Voice loop: Gemini Live API (`gemini-2.0-flash-live`) replaces the entire Optimized Free-Chain (Silero VAD, Groq Whisper STT, Groq Llama in-call, self-hosted Kokoro TTS) with a single native audio-in/audio-out WebRTC session. WebRTC retained as meeting transport (unchanged). Heavy reasoning (async/offline only): Groq Free Tier (Llama 3.3 70B) replaces Ollama/Llama 3.1 for rubric structuring, per-candidate briefs, and hidden-context distillation; OpenRouter Free Tier (NVIDIA Nemotron) replaces Ollama for scorecard synthesis (Extractive Evaluation). Latency target revised: ≤1.5 s P50 / ≤2.5 s P95 (D17 free-chain budget) → **≤800 ms P50 / ≤1.5 s P95** (single-hop WebRTC; measured in S4). AGENT_CONTRACTS.json bumped to v1.2.0 (`gemini_session_config` field added to interviewer task_assign). Risk register: R1 → Gemini Live quota/latency; R2 → concurrent session limits; R15 added (Gemini Live preview instability); R16 added (OpenRouter Nemotron availability). Supersedes D17 voice-stack selection and D17 heavy-reasoning executor (Ollama). | Architectural pivot (user directive) | Adopted — **supersedes D17 voice stack and D17 Ollama executor; refined by D19** |
| 2026-07-13 | D19 | **Hybrid Loop Prosody Enforcement + Model Pinning.** (1) **Model:** Primary voice model pinned to `gemini-3.1-flash-live-preview`; fallback to `gemini-2.5-flash-native-audio` on primary unavailability. (2) **Prosody Policy — Hybrid Loop (MANDATORY; structural enforcement restored):** Gemini Live (`gemini-3.1-flash-live-preview`) features native "Affective dialog" — it processes the emotional style and tone of raw audio and CANNOT be reliably instructed to ignore paralinguistics via system prompt alone. D18's proposed system-prompt enforcement is therefore **rejected** as a compliance risk for unintentional accent/tone bias. Solution: a **two-phase hybrid loop** — Phase 1 (live): `gemini-3.1-flash-live-preview` handles live audio conversation with the candidate and auto-generates a raw text transcript of both sides; Phase 2 (async/offline): that raw text transcript is passed to the Analytics/Scorecard sub-agent (Groq Llama 3.3 70B or OpenRouter Nemotron) which performs ALL evaluation and scoring exclusively on the text — paralinguistic signals are structurally absent from the text transcript, restoring the same class of enforcement as D17's STT boundary. The text transcript IS the blind wall between voice and scorer. Gemini Live is prohibited from producing or transmitting any scoring or competency-level output — it is the conversational interface only. (3) **Latency target confirmed**: ≤800 ms P50 / ≤1.5 s P95 — accounts for realistic network routing physics (Candidate → Google Meet → WebRTC → FastAPI → Gemini API → return); sub-second feel for the candidate while giving the WebRTC bridge realistic breathing room. | Architectural refinement (user directive) | Adopted — **refines D18; restores structural prosody enforcement; pins model** |

Open `[TBD]` values (numbers never invented — set before pilot): confidence threshold; N sourcing cycles before escalation; `[NEEDS_HUMAN_REVIEW]` per-candidate limit; similarity floor; per-turn latency budget (updated in D18 but specific P50/P95 values confirmed in S4); email retry count; cost budget per interview; data retention policy; Gemini Live concurrent session cap; Groq RPM cap for brief generation; k-anonymity threshold.
