# Deliverable 5 — Sprint Plan (6 sprints)

[ASSUME: 2-week sprints, 2–3 engineers; sequencing puts the riskiest component (real-time voice interview loop) at mid-project — DECISIONS.md D6.]

| Sprint | Theme | Key deliverables | Exit criteria |
|---|---|---|---|
| S1 | Foundations | Supabase schema (all Deliverable 2 §2.5 table groups); event bus + envelope validation incl. `voice_context` rule (§4.1); LangGraph supervisor skeleton with stub sub-agents; React/Vite dashboard shell; goal intake (goal + evaluation standard + difficulty level) | Goal persisted; structured rubric written (freeze-on-first-dispatch enforced); Manager→stub→Manager event round-trip visible on dashboard |
| S2 | Sourcing/Screening | Resume parsing → candidate profiles; JD embedding + pgvector ranking; per-batch reports; Manager aggregation + shortlist | Ranked list with similarity scores from a real corpus; zero-qualified escalation path fires |
| S3 | Scheduling + email comms | Google Calendar integration; slot confirm/conflict flow; Communication sub-agent Gmail sends (invite/reject templates) + delivery confirmations; Manager digest email v1 | Candidate invited and slot booked end-to-end; double-conflict escalation reaches user |
| S4 | Voice core (candidate context — Hybrid Loop Phase 1) | WebRTC client deployment + Meet join; **`gemini-3.1-flash-live-preview`** WebRTC session (fallback: `gemini-2.5-flash-native-audio`); native audio conversation + barge-in; Gemini Live auto-transcript generation (both sides); transcript streamed to immutable audit trail; Pre-Flight Sandbox mode + telemetry gate; per-candidate interview brief generation (**Groq Llama 3.3 70B**, async/offline); consent announcement at call start; **Gemini Live prohibited from producing any scoring output** | Test call on a scripted brief completes with live Gemini Live auto-transcript and native barge-in; turn latency instrumented against ≤800 ms P50 / ≤1.5 s P95 (D18/D19; full routing path); sandbox→interview handover verified; negative test: Manager Agent cannot obtain a candidate-context session (voice ownership rule) |
| S5 | Adaptive interviewing + scoring (Hybrid Loop Phase 2) | Full behavioral model (§3.4 steps 0–7); dynamic question generation (no question bank; `gemini-3.1-flash-live-preview` native in-session reasoning); rubric coverage tracking; bias guardrails + protected-territory flagging + prosody policy (D16/D19 — structural via Hybrid Loop text wall); confidence scores + `[NEEDS_HUMAN_REVIEW]`; immutable audit trail incl. `difficulty_estimate`; **Analytics/Scorecard sub-agent receives raw text transcript only (NO audio)** → Extractive Evaluation via Groq Llama 3.3 70B / Groq Llama 3.3 70B (text-only — D19); quote substring validation (D13) | Mock interviews yield post-call report + scorecard with validated verbatim quotes mapped to rubric; 0 competencies scored without evidence; scorecard sub-agent provably received text only (no audio access); audit trail retrievable from dashboard |
| S6 | Manager reporting + hardening | Live reporting meeting (Manager voice — `gemini-3.1-flash-live-preview`, user context); on-demand email query + `[STALE]` flag; Dynamic Context Injection (allowlisted scrape → **Groq Llama 3.3 70B** distillation → enriched JD embedding, D14/D18); Fairness & Bias Lens heatmap (segregated demographics, k-anonymized, D15); all §3.1 escalation rules; failure-mode handling passes; E2E pilot on a real role | Full pipeline goal→decision-email on pilot role; every documented escalation demonstrated; heatmap renders with k-anonymity suppression verified; negative test: Interviewer sub-agent cannot obtain a user-context session; Hybrid Loop verified end-to-end (Gemini Live transcript → text-only scorer → scorecard); risk register review |

Consent/recording legality: consent announcement is a blocking exit criterion in S4 (candidate calls) and S6 (user meetings).
