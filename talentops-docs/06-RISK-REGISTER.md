# Deliverable 6 — Risk Register (rev 2)

Scale: L/M/H (Likelihood, Impact). Owners [TBD].
Consent/recording legality flag: R3 is a launch blocker in two-party-consent jurisdictions; R10/R14 govern special-category demographic data.

| ID | Risk | L | I | Mitigation | Monitor |
|---|---|---|---|---|---|
| R1 | **Gemini Live API latency (D18/D19)** — end-to-end turn latency (Candidate → Google Meet → WebRTC → FastAPI → `gemini-3.1-flash-live-preview` → return) breaches ≤800 ms P50 / ≤1.5 s P95 targets | M | H | Target accounts for full routing physics; WebRTC WebRTC bridge optimization; Gemini Live native processing near-instant; latency measured per-call in S4; fallback to `gemini-2.5-flash-native-audio` on primary model degradation; Pre-Flight Sandbox calibrates per-candidate baseline network RTT | P50/P95 turn latency per call vs D18/D19 SLOs |
| R2 | **Gemini Live concurrent session limits (D18/D19)** — free-tier caps on simultaneous `gemini-3.1-flash-live-preview` sessions degrade or block live calls | M | H | Concurrency cap on simultaneous interviews [TBD]; queue + backpressure on incoming sessions; pre-call quota check; graceful degradation: queue or reschedule rather than hard-fail | Active session count vs free-tier cap; queue depth; session rejection rate |
| R3 | Consent/recording legality — both voice contexts recorded (sandbox included); self-hosted WebRTC = no vendor DPA, compliance fully in-house | M | H | Consent announced at start of every WebRTC call, before the sandbox; interview invalid unless `consent_acknowledged=true`; jurisdiction checklist pre-pilot; in-house recording-retention controls | 100% calls with consent ack |
| R4 | Evaluation hallucination — buzzword density inflating scores; adaptive question drift off-rubric | M | H | **Extractive Evaluation (D13):** verbatim quotes substring-validated against the immutable transcript before any score; `insufficient_evidence` instead of inference; every question targets a rubric competency (audit mapping); brief-grounded generation | % competencies scored without validated quotes (must be 0); % questions without competency mapping |
| R5 | Bias guardrail failure — protected-attribute probing or pedigree scoring | L | H | Guardrails in every dispatch; flag+log protected territory; evidence-only scoring; **Fairness & Bias Lens (D15)** as detective control: cohort × difficulty heatmap + drift alerts | Protected-attribute flags; cohort difficulty-drift alerts |
| R6 | STT inaccuracy (accents, domain jargon) — chain-native Whisper transcript IS the audit trail and the scoring substrate | M | M | Whisper-large-v3 via Groq; low-confidence spans flagged → `[NEEDS_HUMAN_REVIEW]`; Extractive Evaluation quote validation surfaces transcript defects | Flagged-span rate; transcript-gap incidents |
| R7 | External dependency concentration — **Groq is the sole remaining hosted API** (STT + in-call LLM); quota/rate-limit exhaustion or outage kills live calls | M | H | Free-tier quota monitoring + pre-call quota check; fallback: local whisper.cpp STT + Ollama degraded mode [ASSUME: reduced quality accepted over hard failure]; graceful termination + partial report + reschedule on hard failure; WebRTC/Kokoro/Ollama self-hosted → no third-party runtime dependency | Groq quota headroom; per-stage error rates; mid-call drop rate |
| R8 | Prompt injection — via resume content, candidate speech, or **scraped hidden context** (untrusted web content entering the embedding pipeline, D14) | M | H | Resumes, transcripts, and scraped content are data, never instructions; distillation isolation (Ollama/Llama 3.1 summarizes, never executes); employer-controlled allowlist only; envelope validation; no tool escalation from candidate context | Injection test suite pass rate |
| R9 | Voice ownership rule breach (Manager Agent in a candidate call, or Interviewer sub-agent in a user meeting) | L | H | Session broker issues voice sessions keyed by `voice_context` (§2.3); message validation (§4.1); negative tests S4/S6; sandbox runs inside the Interviewer session — no new identity (D12) | Broker/validation rejections logged |
| R10 | PII + special-category data — resumes, transcripts, and self-reported demographics in Supabase | M | H | Segregated demographics schema; RLS denies all agent roles; aggregate-only views with k-anonymity (n < k suppressed, k [TBD]); access-logged audit trail; retention policy [TBD] | Access log review; RLS violation attempts |
| R11 | Scheduling stalls — double-conflict loops | M | L | Double-conflict escalation; widen slot window | Conflict rate |
| R12 | Sandbox→interview handover failure or grading-boundary confusion (candidate believes sandbox was graded, or misses that the interview started) | M | M | Explicit boundary announcement; telemetry gate (bad RTT/jitter → reschedule, not degraded interview); sandbox data excluded from evaluation by construction (D12) | Handover failure rate; candidate-reported confusion |
| R13 | **Prosody/accent bias — MITIGATED (D19 Hybrid Loop restores structural enforcement):** `gemini-3.1-flash-live-preview` features native "Affective dialog" and processes paralinguistics in raw audio. System-prompt enforcement was rejected (D18/D19). **Mitigation via Hybrid Loop (D19):** Gemini Live’s auto-generated text transcript — which contains NO audio — is the exclusive input to the Analytics/Scorecard sub-agent. Paralinguistics are structurally absent from the text. Scoring is performed by a separate text-only model (Groq/Llama 3.3 70B). This restores the same structural enforcement class as D17’s STT boundary. Residual risk: accent effects on Gemini Live’s transcript fidelity (similar to D17’s Whisper mistranscription risk) → monitored via Fairness & Bias Lens cohort drift | L | M | Hybrid Loop structural separation (D19); Extractive Evaluation: quote substring validation surfaces transcript fidelity gaps; Fairness & Bias Lens cohort difficulty-drift alerts; `[NEEDS_HUMAN_REVIEW]` on low-confidence spans | Cohort score/difficulty drift; transcript gap rate per cohort; barge-in false-positive rate per cohort |
| R14 | Scraping legality/ToS exposure (Dynamic Context Injection) | L | M | Employer-controlled properties only (own domain, own blog, own GitHub org); robots.txt compliance; provenance log per source URL; no third-party scraping | Allowlist audit; provenance completeness |
